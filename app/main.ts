import Vue from 'vue'
import App from './App.vue'

Vue.config.productionTip = false

// tslint:disable-next-line:no-unused-expression
new Vue({
  el: '#app',
  template: '<App/>',
  components: { App }
})
